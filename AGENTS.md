# Agent Guidelines

- Remember to make atomic commits that group one logical change.
- After code changes, run `npm run build` and `npm run deploy` by default (no prompt needed).
- Keep code quality high: prefer clear, maintainable code and run lint/tests when feasible.
